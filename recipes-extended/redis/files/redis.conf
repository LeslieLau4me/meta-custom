# Systemd 支持（关键配置）
supervised systemd
daemonize no

# 你的自定义配置
port 6239
requirepass inheroot
protected-mode no
bind 0.0.0.0
dir /var/lib/redis
pidfile /run/redis.pid

# 日志输出到 systemd journal，不需要单独文件
# logfile /var/log/redis.log
# 或者设置为空
logfile ""